// .devcontainer/devcontainer.json
{
  "name": "TypeScript + xterm.js",
  "image": "mcr.microsoft.com/devcontainers/typescript-node:22-bookworm",
  "customizations": {
    "vscode": {
      "extensions": [
        "dbaeumer.vscode-eslint",
        "esbenp.prettier-vscode"
      ],
      "settings": {
        "editor.formatOnSave": true
      }
    }
  },

  "forwardPorts": [3000],

  "postCreateCommand": "sudo apt-get update && sudo apt-get install -y python3 build-essential && npm install -g npm@latest && if [ -f package.json ]; then npm install; fi && npm install xterm @xterm/addon-fit @xterm/addon-web-links ws node-pty && npm install -D @types/ws",
  "remoteUser": "node"
}
